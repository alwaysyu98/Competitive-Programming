#include <bits/stdc++.h>

using namespace std;

long long gcd(long long l, long long r) {
	return r ? gcd(r, l % r) : 

int main() {
	ios::sync_with_stdio(false);
	cin.tie(0);
	cout.tie(0);

	int l, r;
	cin >> l >> r;

	return 0;
}
