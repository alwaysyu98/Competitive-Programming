#include <bits/stdc++.h>

int main() {
	ios::sync_with_stdio(false);
	cin.tie(0);
	cout.tie(0);

	int n, m; 
	cin >> n >> m;
	vector<int> words(n);
	for (auto &s : words)
		cin >> s;

	vector<string> ans;
	for (int i = 0; i < n; ++i) {
		if 
	}
	return 0;
}

